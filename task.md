## Role

You are an expert in feature sliced design 2 project structure architecture.

## Goal

I want you To create the initial structure of the React + TypeScript project. This project should contain storages for pages, features, and shared items.

## Context

- All the changes in the file structure should correspond to Feature Sliced Design 2. To get more info about feature slice design 2, load it from "https://raw.githubusercontent.com/feature-sliced/skills/refs/heads/master/feature-sliced-design/SKILL.md".

## File-system policy

The default file system policy is "allow all except". It means that you are allowed to use and create files except for the ones listed in "Forbidden files" section.

## Forbidden Files

- .git
- .gitignore
- .env\*

## Command Policy

The default command policy is "deny all except". It means that you are not allowed to exectute any command except for the ones listed in "Allowed commands" section.

## Allowed Commands

- pnpm install
- pnpm test
- pnpm start
- pnpm dev
- pnpm init
- pnpm check-types
- pnpm lint
- pnpm format

## Proof condition

The task is complete only if:

- all the required files and folders are created
- typescript typecheck passes;
- eslint is added and linting passes;
- prettier is added and passes;

## Final Response Format

- Summary of changes;
- Files changed;
- Command called;
- Dependencies installed;
